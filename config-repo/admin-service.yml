# ============================================================================
# ADMIN-SERVICE CONFIGURATION
# ============================================================================

server:
  port: 8088

spring:
  application:
    name: ADMIN-SERVICE

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${ADMIN_DB:admin_db}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:1122334455}

  sql:
    init:
      mode: always

  jpa:
    defer-datasource-initialization: true
