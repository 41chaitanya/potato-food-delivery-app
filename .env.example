# ============================================================================
# ENVIRONMENT VARIABLES FOR MICROSERVICES
# ============================================================================
# Copy this file to .env and update values for your environment
# Local development uses default values (after colon)
# Production: Set these in your deployment platform (AWS, Docker, K8s, etc.)
# ============================================================================

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=1122334455

# Service-specific databases
USER_AUTH_DB=user_auth_db
RESTAURANT_DB=restaurant_db
MENU_DB=menu_db
CART_DB=cart_db
ORDER_DB=order_db
PAYMENT_DB=payment_db
DELIVERY_DB=delivery_db
ADMIN_DB=admin_db

# =============================================================================
# SERVICE DISCOVERY (EUREKA)
# =============================================================================
EUREKA_URI=http://localhost:8761/eureka

# =============================================================================
# CONFIG SERVER
# =============================================================================
CONFIG_SERVER_URI=http://localhost:8089

# Config Server Git Backend (for production)
# CONFIG_GIT_URI=https://github.com/your-org/microservice-config.git
# CONFIG_GIT_BRANCH=main
# CONFIG_GIT_USERNAME=your-github-username
# CONFIG_GIT_PASSWORD=your-github-token

# =============================================================================
# JWT CONFIGURATION
# =============================================================================
JWT_SECRET=mySecretKeyForJWTTokenGenerationWhichShouldBeAtLeast256BitsLongForHS256Algorithm2024
JWT_EXPIRATION=86400000

# =============================================================================
# PRODUCTION DEPLOYMENT CHECKLIST
# =============================================================================
# 1. Set all DB_* variables to your cloud database
# 2. Set EUREKA_URI to your Eureka server URL
# 3. Set CONFIG_SERVER_URI to your Config Server URL
# 4. Set CONFIG_GIT_* variables for Git-backed config
# 5. Generate a strong JWT_SECRET (min 256 bits)
# 6. Set SPRING_PROFILES_ACTIVE=git on Config Server
# ============================================================================
